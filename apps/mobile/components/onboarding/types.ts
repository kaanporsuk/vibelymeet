// Re-export canonical onboarding types from the shared module.
// This file exists for backwards compatibility with existing native imports.
export {
  type OnboardingData,
  DEFAULT_ONBOARDING_DATA,
} from '@shared/onboardingTypes';
