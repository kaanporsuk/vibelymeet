import ProfileStudio from './ProfileStudio';

/** Profile tab — ProfileStudio is the only implementation (legacy rollback removed). */
export default function ProfileTabScreen() {
  return <ProfileStudio />;
}
