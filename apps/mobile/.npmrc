# Temporary: allow EAS cloud install to succeed despite @daily-co/config-plugin-rn-daily-js
# requiring expo@^54 while the app uses expo@55. Remove when Daily publishes an Expo 55–compatible plugin.
legacy-peer-deps=true
