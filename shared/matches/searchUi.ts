/**
 * Matches search field — visible lead + supporting hint (web + native parity).
 * Search behavior lives in ./search.ts; this file is UI copy only.
 */
export const MATCHES_SEARCH_LEAD = 'Search:';
export const MATCHES_SEARCH_HINT =
  'Name · vibe · intent · city · event · last message';
