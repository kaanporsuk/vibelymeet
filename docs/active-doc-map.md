# Active doc map

Date: 2026-06-09
Purpose: Keep one current execution path visible for native launch closure and make older planning/runbook references explicitly historical.

---

## Start here

0. **Current Video Date leaner-path cleanup (2026-06-09 Daily-room legacy action removal):** source now removes the legacy public/client-facing `daily-room` actions `create_date_room` and `join_date_room` from the active Edge Function action contract and dispatch; branch delta: `docs/branch-deltas/remove-daily-room-legacy-actions.md`. Web/native Video Date room and token acquisition remains `prepare_date_entry` through `prepareVideoDateEntry`; `video_date_transition('enter_handshake')` remains intentionally preserved; provider-side Daily room creation/reuse/verification remains inside `prepare_date_entry` and `ensure_date_room`; `prepare_diagnostic_entry`, `prepare_solo_entry`, `video_date_leave`, `delete_room`, and match-call actions remain intact. Existing `create_date_room_*` provider observability operation labels can remain as shared Daily provider lifecycle internals, but active clients must not send `action: "create_date_room"` or `action: "join_date_room"`.
0. **Current Event Lobby leaner-path cleanup (2026-06-09 session-source removal):** linked Supabase cloud and generated types now remove the temporary `video_sessions.session_source` discriminator via `supabase/migrations/20260609171950_remove_video_sessions_session_source.sql`; branch delta: `docs/branch-deltas/remove-video-sessions-session-source.md`. The migration redefines the active swipe base wrapper so it preserves `super_vibe_consumed` without reading, writing, or returning source metadata, drops `video_sessions_session_source_rec_swipe_only`, and drops the column. Active validation `supabase/validation/event_lobby_active_event_contract.sql` asserts the column and constraint are absent, and `shared/matching/videoSessionSourceRemovalContracts.test.ts` is wired into `npm run test:event-lobby-regression`. Current session creation no longer stores a source discriminator; clients must use `/event/:eventId/lobby` deck/swipe through `swipe-actions`, reciprocal mutual swipe or supported queue promotion, Ready Gate, then Video Date.
0. **Current Event Lobby leaner-path cleanup (2026-06-09 legacy RPC removal):** linked Supabase cloud and generated types now remove deprecated direct queue/session RPCs `find_video_date_match(uuid,uuid)` and `join_matching_queue(uuid,uuid)` from the active backend contract via `supabase/migrations/20260609163130_remove_legacy_queue_session_rpcs.sql`, and remove the remaining legacy cleanup RPC `leave_matching_queue(uuid)` via `supabase/migrations/20260609165218_remove_leave_matching_queue.sql`. Active validation asserts all three RPCs are absent with `to_regprocedure(...) is null`. `drain_match_queue`, `promote_ready_gate_if_eligible`, Ready Gate, Video Date state-machine behavior, and post-date survey behavior remain intact. Old applied migrations and historical audits may still describe the previous callable compatibility posture; active source, generated types, validation requiring callability, or tests preserving callable behavior are blockers.
0. **Current Event Lobby simplification (2026-06-09 Mystery Match removal):** source and linked Supabase cloud remove Mystery Match as an active product/backend path. Web/native Event Lobby no longer import `useMysteryMatch`, empty-deck UI only exposes refresh/end-break behavior, shared empty-deck state no longer carries `showMysteryMatch`, and active Mystery Match analytics constants were removed. Migration `supabase/migrations/20260609152000_remove_mystery_match.sql` deleted existing test `session_source = 'mystery_match'` sessions and dependents, dropped the public/helper `find_mystery_match` RPC chain, and temporarily constrained/defaulted the now-removed source marker to `reciprocal_swipe`. Residual Mystery Match references are allowed only in old migrations, historical/archive/audit docs, `_cursor_context` snapshots, current removal docs, or absence/assertion tests; active product source, generated types, validation that requires the RPC, or tests preserving the feature are blockers. The supported lobby path is now swipe/mutual-match plus supported queue promotion into Ready Gate.
0. **Current Video Date implementation top (2026-06-09 hot-path no-throw shells and Daily same-session adoption):** local source now includes branch delta `docs/branch-deltas/fix-video-date-hot-path-no-throw-daily-adoption.md`, migration `supabase/migrations/20260609130139_video_date_hot_path_no_throw_daily_adoption.sql`, and expanded contract coverage in `shared/matching/videoDateActiveEntryFailsoftShellContracts.test.ts`, `shared/matching/videoDateDefinitiveOwnershipContracts.test.ts`, and `shared/matching/videoDateEndToEndHardening.test.ts`. This addresses the latest failed production run where Ready Gate reached both-ready but `/date/:sessionId` remained in `Opening the room...` / `Still connecting...` and Network showed raw/pending active-path failures around transition, launch-latency, surface-claim, alive, and joined RPCs. The migration preserves existing active-path bodies behind service-role-only base functions and recreates final no-throw public shells for `claim_video_date_surface`, `mark_video_date_daily_alive`, `mark_video_date_daily_joined`, `video_date_transition`, `video_session_mark_ready_v2`, and `record_video_date_launch_latency_checkpoint`, with last-resort retryable JSON if richer lifecycle exception helpers fail. Web and native/mobile Daily guards now tag fresh call objects with session/room markers and allow route entry, retry, and remount recovery to adopt same-session protected/current calls instead of surfacing `external_call_busy`; Ready Gate prewarm passes markers but does not adopt route-owned active calls, so prewarm TTL/fallback cleanup cannot destroy an active `/date` call; web `/date` also has bounded automatic retry for retryable start failures while the active route shell still owns the date. Verification passed locally with focused active-entry and ownership contracts, PR #1256-#1262 follow-up contract, stable bilateral media contract, `npm run typecheck`, `npm run lint`, `npm run test:video-date:red-flags`, `git diff --check`, and linked Supabase dry-run showing exactly pending migration `20260609130139_video_date_hot_path_no_throw_daily_adoption.sql` at local verification time. This is source/migration implementation evidence until publish close-out applies and verifies linked cloud, clients are redeployed, and a fresh disposable two-user production run proves match -> Ready Gate -> same Daily room -> stable bilateral provider-backed media/date -> date end -> both users persist `date_feedback`, including leave/rejoin and prolonged absence checks.
0. **Current Video Date simplification foundation (2026-06-09 lean runtime contract):** local source now includes `docs/contracts/video-date-lean-runtime-contract.md`, branch delta `docs/branch-deltas/video-date-lean-runtime-contract.md`, shared contract module `shared/matching/videoDateLeanRuntimeContract.ts`, and focused coverage `shared/matching/videoDateLeanRuntimeContract.test.ts`. This adds a small screen/command model over the existing route-decision and snapshot surfaces: `lobby`, `ready_gate`, `date`, `survey`, `done`, and `blocked`, with normalized commands like `swipe`, `mark_ready`, `prepare_date`, `join_date`, `end_date`, and `submit_survey`. It intentionally does **not** add a Supabase migration or new Edge Function; the read boundary remains `get_video_date_start_snapshot_v1`, `video-date-snapshot`, and `get_video_date_snapshot_core` until web/native consumers are migrated. Verification passed with `npx tsx shared/matching/videoDateLeanRuntimeContract.test.ts`, `npm run test:video-date-v4`, and `npm run typecheck`. This is architecture/simplification groundwork only, not product acceptance proof.
0. **Current Video Date implementation top (2026-06-09 active-entry fail-soft shell):** local source now includes branch delta `docs/branch-deltas/fix-video-date-active-entry-failsoft-shell.md`, post-apply repair delta `docs/branch-deltas/fix-video-date-active-entry-join-arg-name-repair.md`, migrations `supabase/migrations/20260609105249_video_date_active_entry_failsoft_shell.sql` and `supabase/migrations/20260609112843_video_date_active_entry_join_arg_name_repair.sql`, and contract coverage `shared/matching/videoDateActiveEntryFailsoftShellContracts.test.ts`, wired into both `npm run test:video-date:red-flags` and `npm run test:video-date-v4`. This implements the recovery from failed production session `d54c5fdb-67f2-4fca-93c5-30936b8af8cb`: web `/date/:sessionId` now owns a stable surface shell as soon as `videoDateAccess === "allowed"` instead of waiting for handshake/date truth, while native/mobile already keeps surface ownership alive through eligible entry/joining/connecting/local-Daily states. `daily-room` maps retryable prepare/confirm payloads away from raw 500 status, and the public active-entry RPCs `video_session_mark_ready_v2`, `video_date_transition`, `mark_video_date_daily_joined`, and `record_video_date_launch_latency_checkpoint` get final no-throw retryable JSON shells. Post-apply live catalog verification found and repaired a public PostgREST argument-name mismatch: `mark_video_date_daily_joined` now preserves the generated/client argument name `p_entry_attempt_id` for its fifth text parameter. Verification passed for the new active-entry contract, stable bilateral media contract, PR #1256-#1262 follow-up contract, fail-soft date-room RPC contract, `npm run test:video-date:red-flags`, `npm run test:video-date-v4`, `npm run typecheck`, `npm run lint`, `git diff --check`, linked Supabase dry-run, linked lint, and linked error-level advisors. Product acceptance still requires a fresh disposable two-user production run through match -> Ready Gate -> same Daily room -> stable bilateral provider-backed media/date -> date end -> both users persist `date_feedback`, including leave/rejoin and prolonged absence checks.
0. **Current Video Date implementation top (2026-06-09 PR #1256-#1262 review-comments follow-up):** source now includes branch delta `docs/branch-deltas/fix-video-date-review-comments-1256-1262-followups.md` and contract coverage `shared/matching/reviewComments1256_1262Followups.test.ts`, wired into both `npm run test:video-date:red-flags` and `npm run test:video-date-v4`. The thread-aware GitHub review-comments pass inspected the last seven merged PRs, `#1256` through `#1262`; no Copilot-authored actionable review threads were found, PR `#1260` was already resolved/outdated, and actionable Codex comments on PRs `#1256`, `#1257`, `#1258`, `#1259`, `#1261`, and `#1262` were addressed. This is source/test/docs hardening only: web surface claims now wait for claimable date/handshake truth and do not build exponential backoff on `SURFACE_NOT_CLAIMABLE`; web/native remote-seen retries preserve the originally accepted render/media evidence source in `p_evidence_source`; native/mobile post-date queue drain uses a runtime ref for fast-changing UI/callback state so verdict UI transitions cannot cancel an already-keyed drain; and review-comment docs now scope older migration/git alignment observations to the exact verification moments that produced them. No Supabase migration or Edge Function changed in this branch. Linked Supabase is still aligned through `20260609045533_video_date_pre_stable_survey_eligibility.sql`; linked dry-run returned `Remote database is up to date`; linked DB lint exited 0 with only legacy warning/notice output; linked error-level advisors returned `No issues found`. This remains **source/cloud implementation evidence only** until updated clients are deployed and a fresh disposable two-user production run proves match -> Ready Gate -> same Daily room -> stable bilateral provider-backed media/date -> date end -> both users persist `date_feedback`, including leave/rejoin and prolonged absence checks.
0. **Current Video Date implementation top (2026-06-09 definitive active media ownership):** source and linked Supabase cloud now include branch delta `docs/branch-deltas/fix-video-date-stable-bilateral-media-gate.md`, migrations `supabase/migrations/20260609014410_video_date_stable_bilateral_media_gate.sql`, corrective `supabase/migrations/20260609022729_video_date_auto_promote_stable_bilateral_media_gate.sql`, corrective `supabase/migrations/20260609031421_video_date_stable_bilateral_media_one_sided_guard.sql`, definitive hardening migration `supabase/migrations/20260609035833_video_date_definitive_active_media_ownership.sql`, and survey-eligibility corrective migration `supabase/migrations/20260609045533_video_date_pre_stable_survey_eligibility.sql`; contract coverage is in `shared/matching/videoDateStableBilateralMediaGateContracts.test.ts`, wired into `npm run test:video-date:red-flags` and `npm run test:video-date-v4`. This implements the failed-session `ec02c212-3cee-4af3-9d4d-dc0e9b846188` recovery plan plus the follow-up devil's-advocate hardening: web `/date` owns from allowed route access instead of waiting for Daily connection state; web Daily start is module-scope coalesced and bounded-retried across route remount/prewarm/retry paths; same-tab web surface claims bridge only hot remount/reload cleanup, layout-syncs its cleanup decision, and releases on terminal/explicit exits; native/mobile date ownership begins pre-join and active surface ownership spans eligible entry, handshake/date, joining, connecting, and local Daily room presence; backend date promotion now requires active bilateral surface ownership plus durable heartbeat-backed copresence or explicit bilateral render-bound remote-seen proof; successful promotion persists `stable_bilateral_media_at`; `state = 'date'` is not an already-good shortcut without stable certification; pre-stable provider absence downgrades to retryable `pre_stable_media_failed` without survey, and shared DB survey-eligibility helpers also exclude that reason; and web/native `PostDateSurvey` confirms the actor's `date_feedback` row before advancing. Linked Supabase is aligned through `20260609045533`; post-apply dry-run is up to date; lint exits 0 with only legacy warnings/notices; error-level advisors report no issues; live catalog markers verified stable columns, active surface claim helper, stable marker helper, preserved absence base, no raw already-date shortcut, pre-stable no-survey downgrade, stable gate surface-claim enforcement, server-side `pre_stable_media_failed` survey ineligibility, `date_feedback` actor-row read support, Daily joined v2 fail-soft wrapper shape, and Daily webhook terminal truth preservation. No web or native build was triggered during the final audit; no-build verification passed via focused stable-media contracts, red-flags, typecheck, lint, full `npm run test:video-date-v4`, and `git diff --check` after canonical Supabase type regeneration. Key lesson: inspect latest deployed function definitions when marker predicates disagree, because same-day corrective migrations replace earlier bodies and wrapper helper generations can move from v1 to v2 without changing the intended behavior. This is **source/cloud implementation evidence only** until updated clients are deployed and a fresh disposable two-user production run proves match -> Ready Gate -> same Daily room -> stable bilateral provider-backed media/date -> date end -> both users persist `date_feedback`, including leave/rejoin and prolonged absence checks.
0. **Video Date strict Daily join and remote-seen proof (2026-06-09 superseded baseline):** source includes branch delta `docs/branch-deltas/fix-video-date-strict-daily-join-remote-seen.md`, migration `supabase/migrations/20260609003604_video_date_strict_daily_join_remote_seen.sql`, and contract coverage `shared/matching/videoDateStrictDailyJoinRemoteSeen.test.ts`, wired into `npm run test:video-date:red-flags` and `npm run test:video-date-v4`. It makes Daily alive/join acceptance require matching provider-session `participant.joined` webhook proof with no newer same-provider-session leave, makes remote-seen require explicit render/media evidence before the existing provider/current-call guard, stops web/native/mobile participant/snapshot hydration from stamping canonical remote-seen, and gates provider-overlap/client auto-promotion through session lifecycle eligibility. That local pass originally recorded `20260609003604` as pending; the current linked Supabase state above has since applied it and advanced through `20260609045533_video_date_pre_stable_survey_eligibility.sql`. This remains **source/migration/test implementation evidence only** until updated clients are deployed and a fresh disposable two-user production run proves both users persist `date_feedback`.
0. **Current Video Date implementation top (2026-06-09 definitive ownership contracts):** source now includes branch delta `docs/branch-deltas/fix-video-date-definitive-ownership-contracts.md` and contract coverage `shared/matching/videoDateDefinitiveOwnershipContracts.test.ts`, wired into `npm run test:video-date:red-flags` and `npm run test:video-date-v4`. This is a source/test guard, not a schema migration: it verifies `both_ready` date ownership without treating it as Daily-start proof, rejects Daily-room metadata and route entry as completion proof, keeps Daily alive/joined/remote-seen evidence stamping owned only by web `/date` and native `/date/[id]`, keeps mark-ready Ready Gate owned, keeps survey details PostDateSurvey owned, and confirms web/native clients do not directly write `date_feedback`. Completion remains only persisted `date_feedback`. Verification passed with the focused ownership contract, red-flag suite, full v4 suite, typecheck, lint, diff check, and linked Supabase dry-run returning `Remote database is up to date.` This is **source/test implementation evidence only** until a fresh disposable two-user production run proves both users persist `date_feedback`.
0. **Current Video Date implementation top (2026-06-09 PR #1242-#1256 review-comments follow-up):** PR #1257 merged as `4e9f87d7107b92a3e197dc0ded41412a9de951aa`, and linked Supabase cloud is aligned through migration `20260608224048_review_comments_1242_1256_followups.sql`. Source includes branch delta `docs/branch-deltas/fix-video-date-review-comments-1242-1256-followups.md` and contract coverage `shared/matching/reviewComments1242_1256Followups.test.ts`. It handles current Codex review comments from the last 15 PRs: certification exceptions now suppress both missing-feedback warning rows, stale same-session pending-feedback drain callbacks no-op after verdict submission/confirmation, native nonretryable prepare-entry failures stop instead of owning `/date`, parked web Daily singleton reuse stops/transfers the old alive heartbeat, zero-feedback reminders stay scoped to the current survey room, retryable eligibility failures cannot terminalize Ready Gate, remote-seen now requires current server-recorded owner/call heartbeat proof, mark-ready safety failures strip nested auxiliary diagnostics, and the active-owner migration copy has a syntax-only replayability repair. This is **source/cloud implementation evidence only** until a fresh disposable two-user production run proves both users persist `date_feedback`.
0. **Current Video Date implementation top (2026-06-09 certification exception closure):** source and linked Supabase cloud now include migration `20260608215911_video_date_certification_exception_closure.sql`, branch delta `docs/branch-deltas/fix-video-date-certification-exception-closure.md`, contract coverage `shared/matching/videoDateCertificationExceptionClosure.test.ts`, and invariant SQL alignment in `docs/sql/video-date-invariants.sql`. This adds a service-owned, admin-visible `video_date_certification_feedback_exceptions` ledger for known historical failed Video Date rows that are missing `date_feedback` after operator review. The ledger is certification-only: it does not persist `date_feedback`, does not release users from `in_survey`, and is not read by queue drain, Ready Gate routing, PostDateSurvey routing, web EventLobby, native EventLobby, or native notification rescue. `video_date_missing_feedback_operator_diagnostics_v1(...)` keeps the same return shape and still shows missing-feedback rows, but `release_blocker` becomes false for active service-owned exceptions. Verification passed with the focused certification-exception contract, red-flag suite, full v4 suite, typecheck, lint, diff check, linked dry-run/push/post-apply dry-run, linked DB lint, linked error-level advisors, live catalog markers, and linked `npm run check:video-date:invariants -- --warn-as-error`. Current linked diagnostics return zero missing-feedback rows, so no exception row was inserted. This is **source/cloud implementation evidence only** until a fresh disposable two-user production run proves both users persist `date_feedback`.
0. **Current Video Date implementation top (2026-06-09 survey feedback drain guard):** source and linked Supabase cloud now include migrations `20260608211359_video_date_survey_feedback_drain_guard.sql` and corrective `20260608214714_video_date_survey_feedback_gate_lint_repair.sql`, branch delta `docs/branch-deltas/fix-video-date-survey-feedback-drain-guard.md`, and contract coverage `shared/matching/videoDateSurveyFeedbackDrainGuard.test.ts`. This synchronously blocks both `drain_match_queue_v2(...)` and legacy `drain_match_queue(...)` from promoting a user to another Ready Gate while that actor has any same-event survey-eligible ended Video Date without their own `date_feedback` row. The structured result is `pending_post_date_feedback` with survey `next_surface` ownership; web survey, web EventLobby, native survey, native EventLobby, and native notification queued-session rescue route back to `/date/:sessionId` or reopen the verdict step before any Ready Gate callback. The migration also revokes direct authenticated `date_feedback` insert/update/delete and drops old own-row write policies, leaving mandatory verdict writes behind `submit_post_date_verdict_v3` / `post-date-verdict` and optional details behind `update_post_date_feedback_details`. Verification passed locally with the focused survey-feedback guard test, `npm run test:video-date:red-flags`, full `npm run test:video-date-v4`, `npm run typecheck`, `npm run lint`, `git diff --check`, linked migration list, linked dry-run, linked public-schema DB lint, linked error-level advisors, and live catalog markers. Supabase cloud is aligned through `20260608214714`; post-apply dry-run returns `Remote database is up to date.` This is **source/cloud implementation evidence only** until deployed to clients and proven by a fresh disposable two-user production run through both users persisting `date_feedback`.
0. **Current Video Date implementation top (2026-06-08 missing feedback certification closure):** PR #1252 merged as `44440923679140dc375fbe4e40ddf749658e920f`; Supabase cloud is aligned through migration `20260608202749_video_date_missing_feedback_certification_closure.sql`; `post-date-verdict-reminders` is deployed as active version `306` updated `2026-06-08 21:01:07 UTC`. Source includes branch delta `docs/branch-deltas/fix-video-date-missing-feedback-certification-closure.md` and contract coverage `shared/matching/videoDateMissingFeedbackCertificationClosure.test.ts`. This closes the zero-feedback survey-required gap by adding service-owned `post_date_zero_feedback_reminders`, service-only claim/stale/result/diagnostic RPCs, and `post-date-verdict-reminders` support for canonical `/date/:sessionId` feedback reminder pushes when neither participant has submitted `date_feedback`. Certification now requires `npm run check:video-date:invariants -- --warn-as-error`; the stale live session `3fabfd4e-523d-4593-bda5-ab6aa20f1005` correctly blocks certification under that mode. Verification passed locally and against linked Supabase, including full `npm run test:video-date-v4`, `npm run typecheck`, `npm run lint`, post-apply dry-run, linked DB lint, linked DB advisors, function inventory, live catalog marker checks, and invariant checks. This is **source/cloud evidence only**, not product-health proof, until a fresh disposable two-user production run completes through both users persisting `date_feedback`.
0. **Video Date both_ready definitive owner / eligibility patch (2026-06-08):** source and linked Supabase cloud include migration `20260608193915_video_date_both_ready_definitive_owner_eligibility.sql`, branch delta `docs/branch-deltas/fix-video-date-both-ready-definitive-owner-eligibility.md`, and contract coverage `shared/matching/bothReadyCanonicalDailyRoomDefinitiveOwner.test.ts`. This wraps the public Ready Gate/date hot-path RPCs already used by web/native/mobile so non-ended `both_ready` returns `/date/:sessionId` ownership even while Daily provider work is pending/degraded, participant eligibility is rechecked before provider room/token work, `date_starting` notifications are fail-soft after the ready commit, terminal survey truth remains date-stack owned until feedback persists, and service-only diagnostics expose stuck `both_ready`/Daily/remote-seen/promotion/survey categories. This is **source/cloud implementation evidence only** until a fresh disposable two-user production run proves both users persist `date_feedback`.
0. **Video Date active owner / terminal truth patch (2026-06-08):** source and linked Supabase cloud include migration `20260608171837_video_date_active_owner_terminal_truth.sql` and docs/tests for the repeated route-owner failure class: web and native/mobile event lobbies yield to an active `/date` owner or terminal survey truth; delayed Daily provider join/left webhook facts are preserved by `occurred_at` even after terminal state; terminal audit generation fields make `video_sessions`, surface-claim audit rows, and `ended_reason` reconstruct one chronological story; and HTTP/PostgREST lifecycle probes cover `video_session_mark_ready_v2`, `mark_video_date_daily_alive`, `claim_video_date_surface`, and `video_date_transition` for duplicate, invalid, and seeded terminal cases. This is **source/cloud implementation evidence only** until a fresh disposable two-user production run proves both users persist `date_feedback`.
0. **Current Video Date implementation top (2026-06-08 active-date stability patch):** latest local source after PR #1248 fixes the repeated `/date` opening/warm-up/date churn class by preserving web active Daily identity/heartbeat during parked same-session remounts and making post-encounter peer-missing automatic terminalization server-owned across web/native/mobile. Historical bilateral encounter truth now logs `daily_no_remote_watchdog_historical_truth_suppressed`, emits `peer_missing_suppressed_remote_seen`, and keeps clients recoverable while provider/server absence reconciliation owns terminal state. Post-encounter peer-missing effects emit `post_encounter_peer_missing_terminal_end_suppressed` / `provider_absence_server_owned_after_encounter`; explicit user exits may still use `partner_absent_after_confirmed_encounter`. Linked Supabase dry-run returned `Remote database is up to date`; no new migration was needed for this patch.
0. **Provider-bound remote-seen baseline (2026-06-08):** provider-bound remote-seen recovery after failed session `34ed864c-e6eb-4804-bc71-8aeba6bce9b1`, event `86dc1e15-d2cc-45f6-be81-628bd685a759`, landed in PR #1245 as squash commit `a178e1265001f01d5beca0375c38a9cb8c0d4e59`. It adds migration `20260608120000_video_date_provider_bound_remote_seen.sql` plus corrective migrations `20260608121834_video_date_remote_seen_identifier_hygiene.sql` and `20260608122623_video_date_remote_seen_lint_cleanup.sql` on top of the PR #1232-#1242 review-comments follow-up. Remote-seen is no longer session-only evidence: web and native/mobile must pass current Daily owner/call/provider proof, and stale provider sessions return structured no-op JSON before the old canonical mutator can change encounter truth. Supabase cloud was verified aligned through `20260608122623_video_date_remote_seen_lint_cleanup.sql`; post-merge dry-run returned `Remote database is up to date.`
0. **Video Date recovery command center:** `docs/video-date-success-command-center.md` — mandatory first read/update point for all Ready Gate, Video Date, Daily, notification, and post-date survey recovery work. It records PR #1190/#1192/#1194/#1196/#1199/#1200/#1212/#1213/#1216/#1218/#1223/#1225/#1230/#1233/#1235/#1240/#1245 plus PR #1219/#1220/#1221/#1222/#1226 documentation/handoff syncs, the current terminal-survey lifecycle hardening, surface-owner recovery work, lifecycle false-away follow-up, 2026-06-06 single-owner runtime hardening, shared entry/Daily owner rollout, stable-copresence heartbeat guard, rollout documentation sync, provider-authoritative presence follow-up after failed session `c9dc7af1-1f40-431f-93ed-4435019126aa`, the 2026-06-07 review-comments follow-up for PR #1205 through #1216 and PR #1217 through #1231, the 2026-06-08 review-comments follow-up for PR #1232 through #1242, provider-terminal recovery after failed session `98d50175-1c75-4966-a6e6-f444c4631289`, Mutual Match handoff closure, provider-backed joined/absence-terminal recovery after failed session `fd02e8ed-a272-46b1-a961-b130e83ce2a4`, routeable `both_ready` entry protection after failed session `916f8ed7-15a9-45ec-8ec2-79879fa60a7f`, Video Session Created definitive contracts, lifecycle RPC terminal contracts, Ready Gate entry proof, provider-overlap promotion after failed session `09c8d1fb-b622-4f7f-ba4c-9f9c66c5e617`, Daily owner definitive recovery after failed session `690f917e-f2d4-4e8f-a9ec-ece5ec70926e`, 2026-06-08 route lifecycle / last-resort RPC fail-soft recovery after failed session `176d9ed3-f1c6-48e4-901a-c2098ed61b34`, and PR #1245 provider-bound remote-seen recovery after failed session `34ed864c-e6eb-4804-bc71-8aeba6bce9b1`. Current implementation baseline for the review-comments follow-up is migration `20260608114500_review_comments_1232_1242_followups.sql` plus corrective identifier hygiene migration `20260608114600_review_comments_identifier_hygiene.sql`, on top of PR #1240 merge `0b4d0db5ae37bea3e322b4de5935fce48362ff87` and migration `20260608080938_video_date_lifecycle_rpc_last_resort_failsoft.sql`; it fixes stale Ready Gate date-route ownership, native parked Daily call scoping, Golden Flow certification scripts, survey-required invariants, sanitized mark-ready safety failures, provider-absence idle resume status, and the short provider-absence base helper `vd_absence_review_1232_1242_base`. Docs-only PRs can advance source `main`, so verify exact source with `git rev-parse HEAD` and `git ls-remote origin refs/heads/main`; do not confuse later docs-only source movement with implementation baselines. The prior review-comments recovery point was verified/applied remotely through `20260608114600_review_comments_identifier_hygiene.sql` after `20260608080938_video_date_lifecycle_rpc_last_resort_failsoft.sql`; the current Supabase top is the provider-bound remote-seen overlay above, verified through `20260608122623_video_date_remote_seen_lint_cleanup.sql`. The command center also preserves the older Video Date migration chain, Daily start ownership, latest-state presence, canonical remote-seen latest-state repair, immediate confirmed-encounter promotion, caller-bound confirmed-encounter promotion wrapper authority, lifecycle-away suppression/clearing for active `web_beforeunload` and `web_pagehide`, current unexpired surface-claim evidence at reconnect expiry, current-peer-vs-historical-encounter separation, sticky survey status until feedback, pending-survey registration repair, terminal Daily room repair/backfill, cleanup/outbox provider-delete markers that preserve room forensics, cross-surface active video/survey route ownership with web/native forced survey guard bypass, native notification pending-survey deep-link recovery, exposed lifecycle RPC outer fail-soft wrappers, append-only surface-claim and presence audit events, widened client stuck observability, terminal-survey hard-stop, provider-backed joined/absence reconciliation, the distinction between first Daily join latency and stable current provider-backed copresence after early leave/rejoin churn, routeable `both_ready` prepare-entry lease protection before provider work, lifecycle RPC terminal/survey recovery after stale terminal truth, shared provider-overlap date promotion, current one-sided remote-seen validation, tokenless active snapshot fallback, same-session Daily remount parking/reuse, native explicit active-handoff cleanup semantics, sanitized client fail-soft payloads, registration-driven survey route dominance, final last-resort fail-soft shells for hot date-room RPCs, sync/deploy evidence, a fresh-session handoff prompt, and the still-unproven manual match -> survey acceptance run.
   - **Latest 2026-06-06 overlay:** migration `20260606092944_video_date_decisive_mark_ready_commit.sql` plus lint cleanup `20260606100511_video_date_mark_ready_lint_cleanup.sql` is now the current Ready Gate mark-ready baseline. It replaces the public `video_session_mark_ready_v2` with a direct commit path so readiness and deterministic `both_ready` room metadata are durable before observability/outbox work; web and native/mobile retry mark-ready RPC errors with the same deterministic idempotency key. This closes the `cac485cd-da3b-475b-aa4c-27b70cd914d6` `57014` / `ready_b` expiry class in code/cloud, but it is still not the manual product acceptance proof.
   - **Owner/stable-copresence overlay:** code now adds shared date-entry owner and Daily owner contracts across web/native/mobile plus owner heartbeats through new RPC `mark_video_date_daily_alive(...)`. Migration `20260606180000_video_date_stable_copresence_handshake_guard.sql` is applied to Supabase cloud and adds service-only `video_date_presence_events` plus a stable-copresence guard that blocks handshake/date start until stable owner-heartbeat copresence or remote-seen proof.
   - **Provider-authoritative follow-up:** applied migrations `20260606203000_video_date_provider_authoritative_presence.sql` and `20260606205211_video_date_provider_participant_id_presence_repair.sql` make stable copresence and Daily alive join-stamping require current provider presence, read Daily webhook `provider_participant_id` before payload fallbacks, while web/native/mobile send `owner_state='joined'` only when Daily is `joined-meeting` with a local provider session id. They are deployed to Supabase cloud but not acceptance-proven yet.
   - **Review-comments follow-up:** applied migration `20260606212727_review_comments_1205_1216_followups.sql` binds authenticated confirmed-encounter promotion to `auth.uid()`, records surface-claim audit outcomes from `ok`/`success` and conflict codes, and restores event-wide Ready Gate inactive cleanup before the decisive mark-ready base. Web/native source preserves active alive-heartbeat ownership when parking live same-session Daily calls, keeps destructive heartbeat cleanup for non-parked/terminal cleanup, and waits for native surface client identity before prejoin. Its former Mystery Match polling note is superseded by the 2026-06-09 removal entry above.
   - **Provider-terminal recovery:** applied migration `20260606224200_video_date_provider_terminal_recovery.sql` and matching web/native changes make no-provider Daily alive calls bounded telemetry, preserve first provider-backed join evidence, skip the RPC client-side until Daily has `joined-meeting` plus a provider session id, stop alive heartbeats on terminal server truth, and recover terminal survey from `event_registrations.queue_status='in_survey'` when the session-row fetch fails. This still needs deployed clients and the fresh two-user production acceptance run.
   - **Provider-backed absence terminal and latest fd02 lesson:** applied migration `20260607103100_video_date_provider_joined_absence_terminal.sql` makes `mark_video_date_daily_joined(...)` a provider-backed facade, reconciles Daily webhook and reconnect-expiry provider absence, preserves short leave/rejoin with a 12-second provider-absence grace, and terminalizes confirmed encounters to survey when both providers remain absent. The `fd02e8ed-a272-46b1-a961-b130e83ce2a4` audit shows `both_ready` to stable copresence can be long because clients initially joined Daily within 3-5 seconds, both left around +26 seconds, then rejoined at +36/+55 seconds; future audits must separate first Daily entry from stable current provider-backed copresence.
   - **Routeable both-ready entry protection:** applied migration `20260607123952_video_date_routeable_both_ready_entry_protection.sql` protects a `both_ready` Ready Gate handoff for five minutes even when deterministic Daily room metadata already exists, extends `ready_gate_expires_at`, makes `daily-room` persist routeable handshake state before Daily provider verification/token minting, preserves terminal room metadata for date-entry timeout diagnostics, and teaches web/native Ready Gate overlays to navigate to `/date/:sessionId` if canonical truth is already routeable after a retryable prepare failure.
   - **Video Session Created definitive contracts:** migration `20260607152000_video_session_created_definitive_contracts.sql` closed active-session `both_ready` date ownership, queued `current_room_id` non-actionability, service-only registration/session drift validation plus dry-run repair, and fail-soft notification/outbox contracts. Its historical Mystery Match payload compatibility and temporary `video_sessions.session_source` type drift are superseded by the 2026-06-09 Mystery Match and session-source removal migrations.
   - **Ready Gate entry proof:** migration `20260607183000_video_date_ready_gate_entry_proof.sql` adds durable per-participant Ready Gate entered timestamps plus service-readable append-only ledger `video_date_ready_gate_entries` and RPC `record_video_date_ready_gate_entered_v1(...)`. Web/native Ready Gate overlays record proof only after hydrated actionable Ready Gate state (`ready`/`ready_a`/`ready_b`/`snoozed`), and first participant entry may extend the active gate to at least 45 seconds from true client mount without marking Ready/date lifecycle truth. This is source/schema closure for the "both users entered" proof gap, not manual acceptance proof.
   - **Lifecycle RPC terminal contracts:** migration `20260607155414_video_date_lifecycle_rpc_terminal_contracts.sql` wraps exposed lifecycle RPCs with shared terminal context and fail-soft JSON so stale/duplicate/already-terminal `mark_video_date_daily_joined`, `video_date_transition`, Daily alive, remote-seen, surface-claim, and vibe-question calls do not leak raw 500s or keep retrying after survey truth. Follow-up migration `20260607183100_video_date_lifecycle_truthy_helper_alignment.sql` adds the tolerant JSON boolean helper and replaces shared payload helpers so malformed/stringy terminal booleans cannot reintroduce raw failures. Web and native/mobile use shared `videoDateLifecycleRpc` classifiers to stop terminal heartbeats/retries and route terminal survey truth through existing post-date survey recovery; generic `session_ended` is terminal-stop truth only, while survey recovery requires `queue_status = 'in_survey'`, `survey_required`, or `surveyRequired`.
   - **Review-comments 1217-1231 follow-up:** migrations `20260607185652_review_comments_1217_1231_followups.sql` and `20260607190533_review_comments_1217_1231_lint_repair.sql` supersede the previous `20260607183100` cloud top for Video Date review-comment handling. They precheck mark-ready participant/service authority before event cleanup, clear provider-absence reconnect grace on Daily rejoin, exclude queued sessions from drift validation/repair, limit terminal lifecycle context for nonparticipants, gate native routeable truth recovery to retryable prepare failures, and repair the first follow-up migration's invalid `video_date_surface_claims.release_reason` reference. Supabase cloud is applied/aligned through `20260607190533`; this remains implementation evidence only.
   - **Provider-overlap promotion:** applied migrations `20260607194546_video_date_definitive_provider_overlap_promotion.sql` and `20260607205617_video_date_provider_overlap_current_remote_seen.sql` after failed session `09c8d1fb-b622-4f7f-ba4c-9f9c66c5e617` make stable copresence tolerate small provider join/heartbeat skew, add shared server date-promotion authority to Daily alive/joined/remote-seen/handshake hot paths, preserve survey continuity for server-started provider-backed dates, keep `both_ready` routeable in startup truth, require one-sided remote-seen evidence to be current for the participant's latest provider-active window, and make `video-date-snapshot` return retryable tokenless active snapshots on non-rate-limit token/provider failure. Supabase cloud is aligned through `20260607205617`, and `video-date-snapshot` is deployed as active version `53`; this remains implementation/cloud evidence only until the manual two-user acceptance run succeeds.
   - **Daily owner definitive recovery:** PR #1235 migrations `20260607222923_video_date_daily_owner_definitive_recovery.sql` and `20260608001000_video_date_base_failsoft_payload_sanitization.sql` plus web/native source changes after failed session `690f917e-f2d4-4e8f-a9ec-ece5ec70926e` make live same-session Daily remount parking non-expiring on web, make native/mobile Daily preservation explicit through `preserve_active_handoff`, keep abort/end/background/timeout cleanup destructive, add `daily_call_singleton_idle_destroy` observability if an idle destroy path is ever re-enabled, and restore final outer fail-soft wrappers for `mark_video_date_daily_alive(...)`, `mark_video_date_daily_joined(...)`, and `video_date_transition(...)` after provider-overlap made those public bodies outermost again. Raw SQL exception diagnostics remain in service-side observability; authenticated web/native/mobile clients receive sanitized fail-soft payloads even when a renamed base RPC returns fail-soft JSON instead of throwing. Supabase cloud is applied/aligned through `20260608001000`, with post-apply dry-run returning `Remote database is up to date`; this is implementation/cloud evidence only until the manual two-user acceptance run succeeds.
   - **Prior PR #1240 source/cloud layer:** verify linked Supabase through `20260608080938_video_date_lifecycle_rpc_last_resort_failsoft.sql`, not only the earlier PR #1235 `20260608001000` Daily-owner corrective migration, when investigating route lifecycle / last-resort fail-soft behavior. Applied migration history is immutable once pushed to cloud; review fixes after cloud apply must land as follow-up migrations.
   - **Route lifecycle / last-resort RPC fail-soft recovery:** PR #1240 after failed session `176d9ed3-f1c6-48e4-901a-c2098ed61b34` makes `event_registrations.queue_status='in_survey'` dominate stale Ready Gate/date route truth, suppresses `/date/:sessionId` -> Ready Gate hydration bounces on owned date routes across web/native/mobile, extends route ownership to survive realistic route churn, preserves web same-session Daily call refs while transferring heartbeat ownership during live remount parking, and adds applied migration `20260608080938_video_date_lifecycle_rpc_last_resort_failsoft.sql` so `claim_video_date_surface(...)`, `mark_video_date_daily_alive(...)`, `mark_video_date_daily_joined(...)`, and `video_date_transition(...)` return sanitized JSON even if delegation, enrichment, sanitizer, or observability fails. PR #1240 merged as `0b4d0db5ae37bea3e322b4de5935fce48362ff87`; linked post-apply dry-run returned `Remote database is up to date`; DB lint exited 0 with warning-only legacy backlog; live catalog checks confirmed final public shells and service-only base/helper grants. This is source/cloud evidence only until the manual two-user acceptance run succeeds.
   - **Review-comments PR #1232-#1242 follow-up:** migrations `20260608114500_review_comments_1232_1242_followups.sql` and `20260608114600_review_comments_identifier_hygiene.sql` are the current follow-up top. They restore Ready Gate canonical redirects when `/date/:sessionId` is not date-capable, scope native parked Daily call reuse to the same session/room, harden Golden Flow certification script parsing/env loading, exclude pre-date ended sessions from survey-required invariant failures, sanitize mark-ready safety-check payloads, preserve idle resume status for inactive provider-absence terminalization, and rename the provider-absence base helper to short catalog name `vd_absence_review_1232_1242_base`.
   - **Provider-bound remote-seen recovery:** PR #1245 / commit `a178e1265001f01d5beca0375c38a9cb8c0d4e59` plus migration `20260608120000_video_date_provider_bound_remote_seen.sql`, corrective `20260608121834_video_date_remote_seen_identifier_hygiene.sql`, and `20260608122623_video_date_remote_seen_lint_cleanup.sql` are the current implementation top after failed session `34ed864c-e6eb-4804-bc71-8aeba6bce9b1`. They make `mark_video_date_remote_seen(...)` require current provider-backed Daily proof, preserve the old canonical mutator as a service-only base, return structured stale-provider no-op JSON for old/session-only clients, update web/native/mobile clients to pass owner/call/provider identity before stamping remote media evidence, restore the Daily alive provider base to short service-only `vd_daily_alive_remote_seen_base`, remove the new remote-seen wrapper lint warning, and were verified on GitHub/Supabase with nested `main` aligned at `a178e1265001f01d5beca0375c38a9cb8c0d4e59` plus parent gitlink commit `7d1443e5a2d6dd93c3bc6df6a0a1810b102c1bc8`.
   - **Documentation/handoff sync:** PR #1219 and later docs-only syncs corrected stale guidance, recorded source/cloud alignment, removed brittle parent-pointer hashes from the handoff, documented that the parent workspace has no remote and should be verified with `git ls-tree HEAD Git/vibelymeet`, and updated this guidance after PR #1233, PR #1235, PR #1240, and PR #1245 so implementation/cloud baselines stay separate from later documentation-only source movement.
1. **Preflight:** from repo root run `npm run launch:preflight`, then `npm run typecheck`.
2. **Operator execution sheet:** `docs/kaan-launch-closure-execution-sheet.md`
3. **Canonical runbook:** `docs/native-launch-closure-master-runbook.md`
4. **Active blocker matrix and evidence log:** `docs/native-final-blocker-matrix.md`
5. **Strict go/no-go:** `docs/native-release-readiness/stage5-release-readiness-and-go-nogo.md`

Use `docs/native-external-setup-checklist.md` for provider/store depth and `docs/native-sprint6-launch-closure-runbook.md` for the phase-by-phase narrative only after starting from the chain above.

---

## Where evidence is recorded

| Evidence type | Canonical file |
|---|---|
| **Video Date both_ready definitive owner / eligibility patch (2026-06-08)** | `docs/video-date-success-command-center.md`; branch delta `docs/branch-deltas/fix-video-date-both-ready-definitive-owner-eligibility.md`; migration `supabase/migrations/20260608193915_video_date_both_ready_definitive_owner_eligibility.sql`; contract `shared/matching/bothReadyCanonicalDailyRoomDefinitiveOwner.test.ts`; package wiring in `package.json` |
| **Video Date active-date stability patch after latest repeated opening/warm-up/date churn (2026-06-08)** | `docs/video-date-success-command-center.md`; web lifecycle in `src/hooks/useVideoCall.ts` and `src/pages/VideoDate.tsx`; native/mobile lifecycle in `apps/mobile/app/date/[id].tsx`; contracts in `shared/matching/videoDateWarmupStabilityContracts.test.ts`, `shared/matching/reviewComments1188_1197Followups.test.ts`, `shared/matching/videoDateTerminalSurveyLifecycleHardening.test.ts`, `shared/matching/videoDateLatestFailureRouteLifecycleContracts.test.ts`, and `shared/matching/videoDateStableCopresenceOwnerContracts.test.ts` |
| **Video Date provider-bound remote-seen recovery after failed session `34ed864c-e6eb-4804-bc71-8aeba6bce9b1` (PR #1245, 2026-06-08)** | `docs/video-date-success-command-center.md`; migrations `supabase/migrations/20260608120000_video_date_provider_bound_remote_seen.sql`, `supabase/migrations/20260608121834_video_date_remote_seen_identifier_hygiene.sql`, and `supabase/migrations/20260608122623_video_date_remote_seen_lint_cleanup.sql`; web/native remote-seen callers in `src/hooks/useVideoCall.ts` and `apps/mobile/app/date/[id].tsx`; contract coverage in `shared/matching/videoDateLifecycleRpcFailsoft.test.ts`, `shared/matching/videoDateIdentifierHygieneContracts.test.ts`, and `shared/matching/videoDateEndToEndHardening.test.ts`; source commit `a178e1265001f01d5beca0375c38a9cb8c0d4e59` |
| **Video Date active recovery command center, PR #1190/#1192/#1194/#1196/#1199/#1200/#1212/#1213/#1216/#1218/#1223/#1225/#1230/#1233 deploy/doc-sync state plus review-comments follow-up, latest-state presence, immediate confirmed-encounter promotion, Daily start ownership, sticky survey lifecycle, terminal Daily room repair, cross-surface active video/survey route ownership with forced survey guard bypass, native notification pending-survey deep-link recovery, lifecycle RPC outer fail-soft wrappers, lifecycle false-away suppression for active beforeunload/pagehide, current surface-claim expiry guard, single-owner runtime hardening, append-only surface-claim audit, decisive Ready Gate mark-ready commit, shared date-entry/Daily owner contracts, applied stable-copresence heartbeat migration `20260606180000`, applied provider-authoritative presence migration `20260606203000`, applied provider-participant-id repair migration `20260606205211`, applied review-comments migration `20260606212727`, provider-terminal recovery migration `20260606224200`, mutual-match handoff migration `20260607103000`, provider joined/absence-terminal migration `20260607103100`, routeable both-ready entry protection migration `20260607123952`, video-session-created definitive contracts migration `20260607152000`, lifecycle RPC terminal contracts migration `20260607155414`, Ready Gate entry proof migration `20260607183000`, lifecycle truthy-helper alignment migration `20260607183100`, provider-overlap promotion migrations `20260607194546` and `20260607205617`, Daily owner definitive recovery migration `20260607222923`, base fail-soft payload sanitization migration `20260608001000`, service-only presence ledger, widened client stuck observability, terminal-survey hard-stop, sync evidence, fd02 first-join-vs-stable-copresence lesson, latest routeable-entry/lifecycle-terminal/provider-overlap/Daily-owner handoff lessons, same-session Daily remount preservation, sanitized client fail-soft payloads, and handoff prompt (2026-06-08)** | `docs/video-date-success-command-center.md`; branch deltas `docs/branch-deltas/fix-video-date-ready-gate-decisive-mark-ready.md`, `docs/branch-deltas/fix-video-date-entry-daily-owner-stable-copresence.md`, `docs/branch-deltas/fix-video-date-provider-authoritative-presence.md`, `docs/branch-deltas/fix-video-date-provider-terminal-recovery.md` |
| **Video Date Golden Flow red-flag closure and certification checklists (2026-06-08)** | `docs/qa/video-date-golden-flow-certification.md`; `docs/qa/video-date-native-device-certification.md`; `docs/runbooks/video-date-edge-function-release-verification.md`; invariant SQL `docs/sql/video-date-invariants.sql`; commands `npm run test:video-date:red-flags`, `npm run check:video-date:invariants`, `npm run verify:video-date:functions`, `npm run certify:video-date:golden-flow` |
| **Video Date Sprint 0 baseline and risk map (2026-05-25)** | `docs/audits/video-date-sprint0-baseline-risk-map-2026-05-25.md`; branch delta `docs/branch-deltas/video-date-sprint0-baseline-risk-map.md`; regression assertion `shared/matching/videoDateSprint0BaselineContracts.test.ts` |
| **Branch delta (Video Dates P0/P1 closure)** | `docs/branch-deltas/fix-video-date-p0-p1-closure.md` |
| **Full-system forensic closure audit + cleanup matrix (2026-04-14)** | `docs/audits/full-system-forensic-closure-audit-2026-04-14.md` and `docs/audits/full-system-cleanup-matrix-2026-04-14.md` |
| **Branch delta (forensic audit pass)** | `docs/branch-deltas/audit-full-system-forensic-closure-and-cleanup.md` |
| **Mechanical trust closure (types + inventory + surface audit)** | `docs/audits/mechanical-trust-closure-2026-04-14.md` |
| **Last-mile closure (E2E + orphan triage + deduct_credit review)** | `docs/audits/e2e-minimal-layer-2026-04-14.md`, `docs/audits/orphan-triage-2026-04-14.md`, `docs/audits/deduct-credit-security-review-2026-04-14.md` |
| **`deduct_credit` auth closure (caller map + migration)** | `docs/audits/deduct-credit-caller-map-2026-04-14.md`, `docs/branch-deltas/deduct-credit-security-closure-2026-04-14.md`, migration `20260429100000_deduct_credit_auth_bind.sql` |
| **Deleted video-date components (reverse audit, PR #399)** | `docs/audits/deleted-files-reverse-audit-2026-04-14.md`, `docs/audits/deleted-files-restore-matrix-2026-04-14.md` |
| **SelfViewPIP follow-ups (match-call PIP mount + feedback takeover)** | Closure: `docs/branch-deltas/selfview-pip-followups-closure-2026-04-14.md`. Audits: `docs/audits/selfview-pip-followups-audit-2026-04-14.md`, `docs/audits/selfview-pip-drag-snap-investigation-2026-04-14.md` (snap deferred). Background: deleted-file reverse audit row above (`docs/audits/deleted-files-reverse-audit-2026-04-14.md`) |
| Launch blocker status, build ids, pass/fail updates | `docs/native-final-blocker-matrix.md` |
| Browser/runtime proof results | `docs/browser-auth-runtime-proof-results.md` |
| Post-audit ops checklist (Supabase vs Vercel vs manual QA) | `docs/post-audit-operational-verification-checklist.md` |
| Google TLS certificate posture (Q2 2026 GTS/ECDSA notice) | `docs/audits/google-tls-certificate-posture-2026-06-04.md`; static guard `npm run test:google-tls-posture` |
| Distance Visibility Stage 1 rollout and Stage 2 final enforcement | `docs/distance-visibility-stage1-rollout.md`, `docs/distance-visibility-stage2-final-enforcement.md` |
| Activity Status privacy boundary rebuild delta | `docs/branch-deltas/fix-activity-status-privacy-boundary.md`, `docs/activity-status-privacy-verification.sql` |
| Blocked Users server-owned safety production closure | `docs/branch-deltas/blocked-users-server-owned-safety-production-closure.md` |
| Fresh smoke bootstrap method and proof boundaries | `docs/fresh-smoke-proof-bootstrap.md` |
| Proof policy and rebuild-proof context | `docs/authenticated-proof-and-rebuild-plan.md` |
| Clean rebuild rehearsal log | `docs/rebuild-rehearsal-log.md` |
| Ready Gate registration ownership hardening | `docs/ready-gate-server-owned-registration-status.md`, `docs/ready-gate-server-owned-registration-status-final-audit.md`; migrations `20260501141000_ready_gate_server_owned_registration_status.sql`, `20260501142000_ready_gate_client_lifecycle_overwrite_guard.sql` |
| **Video Date Handshake/active-presence release: timer starts after active latest Daily co-presence, confirmed encounters promote before deadline fallback, survey terminal state persists until feedback, active video/survey owns the route surface including native notification pending-survey taps, lifecycle false-away cannot terminalize active dates, provider-backed Daily presence gates stable copresence, no-provider provider-terminal heartbeats are bounded, provider joined stamps require provider proof, provider absence terminalizes confirmed encounters after grace, routeable handshake state is persisted before provider token work, live same-session Daily calls survive route remount churn, and exposed date/queue/surface RPCs fail soft under owner churn with sanitized client payloads** | `docs/vibely-canonical-project-reference.md` §4.1 and `docs/video-date-success-command-center.md`; latest recovery migrations include `20260606180000_video_date_stable_copresence_handshake_guard.sql`, `20260606203000_video_date_provider_authoritative_presence.sql`, `20260606224200_video_date_provider_terminal_recovery.sql`, `20260607103100_video_date_provider_joined_absence_terminal.sql`, `20260607123952_video_date_routeable_both_ready_entry_protection.sql`, `20260607155414_video_date_lifecycle_rpc_terminal_contracts.sql`, `20260607183100_video_date_lifecycle_truthy_helper_alignment.sql`, `20260607185652_review_comments_1217_1231_followups.sql`, `20260607190533_review_comments_1217_1231_lint_repair.sql`, `20260607194546_video_date_definitive_provider_overlap_promotion.sql`, `20260607205617_video_date_provider_overlap_current_remote_seen.sql`, `20260607222923_video_date_daily_owner_definitive_recovery.sql`, `20260608001000_video_date_base_failsoft_payload_sanitization.sql`, and `20260608080938_video_date_lifecycle_rpc_last_resort_failsoft.sql`; Edge Functions `daily-room`, `video-date-snapshot`, `video-date-outbox-drainer`, `video-date-room-cleanup`, and `video-date-orphan-room-cleanup` |
| **Event Lobby active-event, swipe retry, and web/native lobby gating closure** | `docs/audits/event-lobby-active-event-contract-verification.md`, `docs/audits/event-lobby-swipe-idempotency-verification.md`, `docs/audits/event-lobby-web-gating-verification.md`; branch deltas `docs/branch-deltas/fix-event-lobby-active-event-contract.md`, `docs/branch-deltas/fix-swipe-retry-idempotency-notification-dedupe.md`, `docs/branch-deltas/fix-event-lobby-web-gating.md`; migrations `20260501223000_event_lobby_canonical_active_state.sql`, `20260501224000_event_lobby_swipe_already_swiped.sql`; Edge Function `swipe-actions` |
| **Event Lobby Ready Gate and queued-match contract** | `docs/contracts/event-lobby-ready-queue-contract.md`, `docs/audits/event-lobby-ready-queue-contract-verification.md`; branch delta `docs/branch-deltas/fix-event-lobby-ready-queue-contract.md`; migration `20260501225000_event_lobby_ready_queue_contract.sql` |
| **Event Lobby deck payload and media contract** | `docs/contracts/event-lobby-deck-payload-contract.md`, `docs/audits/event-lobby-deck-payload-media-verification.md`; branch delta `docs/branch-deltas/fix-event-lobby-deck-payload-media.md`; migration `20260501230000_event_lobby_deck_payload_media.sql` |
| **Event Lobby observability taxonomy** | `docs/contracts/event-lobby-observability.md`, `docs/audits/event-lobby-observability-verification.md`; branch delta `docs/branch-deltas/fix-event-lobby-observability.md`; Edge Function `swipe-actions` |
| **Event Lobby regression harness** | `scripts/run_event_lobby_regression.sh`, `docs/golden-path-event-lobby-regression-runbook.md`, `docs/audits/event-lobby-regression-harness-verification.md`; branch delta `docs/branch-deltas/test-event-lobby-regression-harness.md` |
| **Event Lobby registration RLS authority hardening** | Migration `supabase/migrations/20260606164737_event_registration_rpc_owned_dml_lockdown.sql`; validation `supabase/validation/event_registration_rpc_owned_dml_lockdown.sql`; static contract `shared/matching/eventRegistrationRlsAuthority.test.ts`; env-gated runtime proof `shared/matching/eventLobbyDirectWriteRlsRuntime.test.ts` |
| **Event Lobby native/backend contract** | `docs/contracts/event-lobby-native-contract.md`, `docs/audits/event-lobby-native-contract-verification.md`; branch delta `docs/branch-deltas/docs-event-lobby-native-contract.md` |
| **Event Lobby native parity implementation** | `docs/audits/native-event-lobby-parity-implementation.md`; branch delta `docs/branch-deltas/fix-native-event-lobby-parity.md`; regression assertion `shared/matching/nativeEventLobbyContractParity.test.ts` |
| **Event Lobby legacy RPC removal** | Migrations `supabase/migrations/20260609163130_remove_legacy_queue_session_rpcs.sql` and `supabase/migrations/20260609165218_remove_leave_matching_queue.sql`; validation `supabase/validation/event_lobby_active_event_contract.sql`; contracts `shared/matching/eventLobbyCanonicalActiveState.test.ts` and `shared/matching/videoDateClosureIssuesContracts.test.ts`; generated Supabase types no longer expose `find_video_date_match`, `join_matching_queue`, or `leave_matching_queue` |
| **Event Lobby Mystery Match removal** | Migration `supabase/migrations/20260609152000_remove_mystery_match.sql`; removal contract `shared/matching/mysteryMatchRemovalContracts.test.ts`; validation `supabase/validation/event_lobby_active_event_contract.sql`; generated Supabase types no longer expose `find_mystery_match`; read-only verification evidence is in `docs/video-date-success-command-center.md` |
| **Event Lobby session-source removal** | Migration `supabase/migrations/20260609171950_remove_video_sessions_session_source.sql`; branch delta `docs/branch-deltas/remove-video-sessions-session-source.md`; contract `shared/matching/videoSessionSourceRemovalContracts.test.ts`; validation `supabase/validation/event_lobby_active_event_contract.sql`; generated Supabase types no longer expose `video_sessions.session_source` |
| **Daily-room legacy date action removal** | Branch delta `docs/branch-deltas/remove-daily-room-legacy-actions.md`; contract `shared/matching/dailyRoomLegacyActionRemovalContracts.test.ts`; active `daily-room` contract/dispatch no longer accepts `create_date_room` or `join_date_room`; web/native entry remains `prepare_date_entry` |
| **Event Lobby final closure audit** | `docs/audits/event-lobby-closure-report.md`; source status pointer `docs/audits/event-lobby-deck-deep-dive.md`; branch delta `docs/branch-deltas/audit-event-lobby-closure.md` |
| **Event Lobby deep cleanup audit** | `docs/audits/event-lobby-deep-cleanup-audit-2026-05-01.md`; branch delta `docs/branch-deltas/audit-event-lobby-deep-cleanup.md` |
| **Event Lobby batch-1 backend contract investigation closure** | `docs/audits/event-lobby-investigation-batch-1-backend-contracts.md`; branch delta `docs/branch-deltas/fix-event-lobby-investigation-batch-1-backend-contracts-closure.md`; regression assertion `shared/matching/eventLobbyInvestigationBatch1Closure.test.ts` |
| **Provider operational QA: OneSignal push** | `docs/branch-deltas/fix-onesignal-provider-operational-qa.md`; provider sheet `_cursor_context/vibely_onesignal_provider_sheet.md`; regression assertion `shared/matching/onesignalProviderOperationalQa.test.ts` |
| **Provider operational QA: Bunny media** | `docs/branch-deltas/fix-bunny-provider-operational-qa.md`; provider sheet `_cursor_context/vibely_bunny_provider_sheet.md`; regression assertion `shared/matching/bunnyProviderOperationalQa.test.ts` |
| **Provider operational QA: Daily calls** | `docs/branch-deltas/fix-daily-provider-operational-qa.md`; provider sheet `_cursor_context/vibely_daily_provider_sheet.md`; regression assertion `shared/matching/dailyProviderOperationalQa.test.ts` |
| **Provider operational QA: Resend email** | `docs/branch-deltas/fix-resend-email-provider-operational-qa.md`; regression assertion `shared/matching/resendEmailProviderOperationalQa.test.ts` |
| **Provider operational QA: Twilio phone verification** | `docs/branch-deltas/fix-twilio-phone-verification-qa.md`; regression assertion `shared/matching/twilioPhoneVerificationQa.test.ts` |
| **Provider/notification deep tidy audit** | `docs/audits/deep-audit-implemented-work-2026-05-01.md`; branch delta `docs/branch-deltas/chore-deep-audit-implemented-work-tidy.md`; regression assertion `shared/matching/deepAuditImplementedWorkTidy.test.ts` |
| **Current work deep tidy audit** | `docs/audits/deep-audit-current-work-tidy-2026-05-01.md`; branch delta `docs/branch-deltas/chore-deep-audit-current-work-tidy.md`; regression assertion `shared/matching/deepAuditCurrentWorkTidy.test.ts` |
| **Native physical-device QA readiness** | `docs/qa/native-physical-device-qa-runbook.md`; branch delta `docs/branch-deltas/qa-native-physical-device-flow.md`; regression assertion `shared/matching/nativePhysicalDeviceQaReadiness.test.ts` |
| **RevenueCat native entitlement readiness** | `docs/branch-deltas/fix-revenuecat-native-entitlement-readiness.md`; regression assertion `shared/matching/revenueCatNativeEntitlementReadiness.test.ts` |
| **Post-stream provider/native deep audit and tidy** | `docs/audits/post-stream-provider-native-readiness-audit-2026-05-01.md` |
| **Screenshot-led native visual parity capture plan** | `docs/qa/screenshot-led-native-visual-parity-capture-plan.md`; branch delta `docs/branch-deltas/fix-screenshot-led-native-visual-parity.md`; regression assertion `shared/matching/screenshotLedNativeVisualParity.test.ts` |
| **Recent hardening deep audit and cleanup (historical partial pass)** | `docs/audits/recent-hardening-deep-audit-2026-05-01.md`; branch delta `docs/branch-deltas/chore-deep-audit-recent-hardening.md` |
| **Admin P4 growth-scale intelligence** | `docs/p4/metrics-dictionary.md`, `docs/p4/analytics-instrumentation-map.md`, `docs/p4/native-store-operations-playbook.md`, `docs/p4/bi-governance.md`; branch delta `docs/branch-deltas/admin-p4-growth-scale-intelligence.md`; regression assertion `shared/admin/adminP4IntelligenceContracts.test.ts` |

---

## Canonical docs

| Role | File |
|---|---|
| Preflight | `npm run launch:preflight` + `npm run typecheck` |
| **Video Date active recovery source of truth** | `docs/video-date-success-command-center.md` |
| **Video Date survey feedback drain guard local patch** | `docs/branch-deltas/fix-video-date-survey-feedback-drain-guard.md`; migration `supabase/migrations/20260608211359_video_date_survey_feedback_drain_guard.sql`; contract `shared/matching/videoDateSurveyFeedbackDrainGuard.test.ts` |
| **Video Date active owner / terminal truth local patch** | `docs/branch-deltas/fix-video-date-active-owner-terminal-truth.md`; migration `supabase/migrations/20260608171837_video_date_active_owner_terminal_truth.sql`; runtime probe `shared/matching/videoDateLifecycleRpcPostgrestRuntime.test.ts` |
| **Video Date Golden Flow certification** | `docs/qa/video-date-golden-flow-certification.md`; native device proof `docs/qa/video-date-native-device-certification.md`; Edge Function release verification `docs/runbooks/video-date-edge-function-release-verification.md` |
| **Video Date Sprint 0 source baseline and risk map** | `docs/audits/video-date-sprint0-baseline-risk-map-2026-05-25.md` |
| **Architecture, providers, import boundaries (`@shared/*` vs root `shared/`)** | `docs/vibely-canonical-project-reference.md` |
| **Native v1 architecture lock (routes, backend contracts, providers, gap list)** | `docs/native-sprint0-architecture-lock.md` |
| **Ready Gate / video-date registration ownership** | `docs/ready-gate-server-owned-registration-status-final-audit.md` |
| **Ready Gate and Event Lobby backend contract** | `docs/ready-gate-backend-contract.md` |
| **Sprint 5 launch-polish triage (static matrix + implemented handoff fixes)** | `docs/native-sprint5-launch-polish-triage.md` |
| Operator execution sheet | `docs/kaan-launch-closure-execution-sheet.md` |
| Canonical launch-closure runbook | `docs/native-launch-closure-master-runbook.md` |
| Active launch backlog and blocker matrix | `docs/native-final-blocker-matrix.md` |
| Strict release-readiness decision | `docs/native-release-readiness/stage5-release-readiness-and-go-nogo.md` |
| Provider and store setup depth | `docs/native-external-setup-checklist.md` |
| **Native external setup closure gate** | `docs/native-external-setup-closure.md` |
| Phased operator detail | `docs/native-sprint6-launch-closure-runbook.md` |
| **Web regression harness (static + manual checklist)** | `scripts/run_golden_path_smoke.sh` → `docs/golden-path-regression-runbook.md` |
| **Event Lobby regression harness (focused static + staging checklist)** | `scripts/run_event_lobby_regression.sh` → `docs/golden-path-event-lobby-regression-runbook.md` |
| **Event Lobby native/backend contract** | `docs/contracts/event-lobby-native-contract.md` |
| **Native RC smoke pack (iOS/Android operator checklist)** | `docs/qa/native-rc-smoke-pack.md` |
| **Native physical-device QA runbook** | `docs/qa/native-physical-device-qa-runbook.md` |
| **Screenshot-led native visual parity capture plan** | `docs/qa/screenshot-led-native-visual-parity-capture-plan.md` |
| **Provider readiness evidence pack (OneSignal, Bunny, Daily, Resend, Twilio, RevenueCat)** | Stream deltas in `docs/branch-deltas/` plus assertions in `shared/matching/*ProviderOperationalQa.test.ts` and `shared/matching/revenueCatNativeEntitlementReadiness.test.ts` |
| **Web push / OneSignal production verification** | `docs/web-push-production-checklist.md` |
| **Native runtime provider hardening (push boundary + iOS React source-build fix)** | `docs/native-runtime-provider-hardening.md` |
| **Authenticated proof / rebuild policy** | `docs/authenticated-proof-and-rebuild-plan.md` |
| **Rebuild rehearsal evidence log** | `docs/rebuild-rehearsal-log.md` |
| **Repo hardening / dead-surface closure (dated)** | `docs/repo-hardening-closure-2026-04-11.md` |
| **Final closure sprint report (branch isolation + ESLint + proof boundaries)** | `docs/hardening-final-closure-sprint-2026-04-11.md` |
| **Current-email OTP (Edge + web/native parity + secret/HMAC semantics)** | `docs/email-verification-settlement-2026-04-11.md` |

**Singular backlog framing:** for launch closure, the only active backlog/evidence log is `docs/native-final-blocker-matrix.md`. Older sprint boards, deferred backlogs, and parity plans are historical context only unless this map or a canonical doc explicitly promotes them again.

---

## Historical or superseded docs

These remain in-repo for audit history, provenance, or deep context, but they are **not** active entrypoints for launch closure:

- `docs/native-launch-readiness.md` — historical pre-consolidation readiness summary
- `docs/native-deployment-validation-sequence.md` — superseded by the current execution-sheet/runbook chain; contains stale branch naming
- `docs/native-v1-rc-operator-runbook.md` — supplemental RC validation workflow, not the launch-closure entrypoint
- `docs/native-sprint-board.md` — historical implementation backlog
- `docs/native-deferred-runtime-bugs-backlog.md` — historical deferred backlog, not the active launch backlog
- `docs/native-web-handoff-burndown.md` — historical scope/handoff reference, still cited only for accepted web handoffs
- `_cursor_context/vibely_rebuild_runbook.md` — canonical for frozen web rebuild only, not native launch closure (banner at top notes 2026-04-11 removals)
- `_cursor_context/Native_Build_Beginning_Handoff.md` — historical native kickoff handoff; superseded by the agent-neutral current docs in this map
- `_cursor_context/vibely_cursor_hardening_campaign.md` — historical filename retained for provenance; content now functions as an agent-neutral rebuildability brief
- `_cursor_context/vibely_discrepancy_report.md` — historical rebuild audit
- **`_cursor_context/vibely_golden_snapshot_audited.md`** and **`_cursor_context/vibely_rebuild_runbook.md`** — include a **2026-04-11** alignment note for `/ready/:readyId` → `ReadyRedirect` and removed unrouted surfaces; still **verify** against `src/App.tsx` for any older § inventory counts. **Other `_cursor_context/*.md` files** — audit/snapshot provenance; some may still name Lovable-era hosting or pre-removal paths. Do not treat them as current route or deploy truth without cross-checking `docs/repo-hardening-closure-2026-04-11.md` and the live `src/App.tsx` route table.
- `docs/_archive/historical/vibely_golden_snapshot_audited_duplicate_2026-04-11.md` — archived duplicate copy; use `_cursor_context/vibely_golden_snapshot_audited.md`

---

## Branch and source-of-truth note

- Branch names shown inside older phase docs are provenance only; do not treat them as the required current working branch or branch base.
- For legacy parity/planning docs, "web as source of truth" now means historical design-reference context only. Current launch-closure truth is the shared backend/runtime state plus the canonical docs listed above.
