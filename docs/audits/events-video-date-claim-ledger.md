# Events + video-date claim ledger

**Investigation date:** 2026-04-11  
**Repo HEAD:** current workspace at investigation time  

**Note on “the attached audit”:** `Pasted text.txt` was **not present** in the workspace. Claims below are taken from **`docs/events-hardening-phase1-release-audit.md`**, **`docs/events-hardening-phase2-release-audit.md`**, **`docs/events-hardening-phase3-release-audit.md`**, **`_cursor_context/vibely_golden_snapshot_audited.md`**, **`_cursor_context/vibely_migration_manifest.md`**, and **`docs/supabase-full-backend-vs-frontend-audit.md`**, which together form the narrative this pass challenges.

**Label key:** `VERIFIED IN CODE` · `VERIFIED IN MIGRATION` · `PRESENT IN HEAD, NOT IN FROZEN ZIP BASELINE` · `DOC ONLY / STALE DOC` · `INFERRED / UNPROVEN` · `CONTRADICTED`

| Audit claim (paraphrase) | Exact source | Evidence | Frozen baseline (pre–2026-03-11 zip) | Current HEAD | Confidence | Why it matters |
|--------------------------|--------------|----------|--------------------------------------|--------------|------------|----------------|
| **`swipe-actions` Edge Function is the active swipe path** | Phase 3 audit; `useSwipeAction.ts` | `supabase/functions/swipe-actions/index.ts`; `src/hooks/useSwipeAction.ts` invokes `swipe-actions` | **NOT listed** in `_cursor_context/vibely_golden_snapshot_audited.md` §6 function inventory (28 names, no `swipe-actions`) | **VERIFIED IN CODE** | High | If “March 10 frozen web baseline” is equated to §6 inventory, **`swipe-actions` is missing from that doc** while code has used EF for a long time (`git log` shows history to Stream 2E). |
| **`handle_swipe` is invoked only via `swipe-actions` (not direct RPC from app)** | `docs/supabase-full-backend-vs-frontend-audit.md` | Web: `useSwipeAction` → EF only. No `supabase.rpc('handle_swipe')` in `src/` | INFERRED: frozen zip types listed `handle_swipe` as RPC | **VERIFIED IN CODE** | High | Establishes **indirection**: server still owns RPC; client never calls `handle_swipe` directly on web. |
| **`ready_gate_transition` exists and owns ready/snooze/forfeit** | Phase 1 audit; migration manifest Stream 2B | `supabase/migrations/20260311153000_ready_gate_transition.sql`; superseded by `20260404195500_*`, `20260412143000_*` | **Not in frozen zip** per manifest (last frozen migration `20260310124838`; Stream 2B is `20260311153000`) | **VERIFIED IN CODE + MIGRATION** | High | **Chronology:** server-owned Ready Gate is **post–frozen-archive**, not “March 10 zip contents.” |
| **`video_date_transition` owns date lifecycle** | Migration manifest Stream 2A | `20260311133000_video_date_state_machine.sql` + later `20260404140000_*`, `20260409100000_*` | Same as above — **after** frozen zip cutoff | **VERIFIED IN CODE + MIGRATION** | High | Same chronology point as Ready Gate. |
| **`mark_lobby_foreground(p_event_id)` added; only true lobby stamps foreground** | Phase 1 audit | `supabase/migrations/20260404191500_phase1_1_true_lobby_foreground.sql`; `src/pages/EventLobby.tsx` calls RPC with visibility + route gates | **Not in March 10 zip** (April 2026 migration) | **VERIFIED IN CODE** | High | Confirms **April hardening**, not March audited archive. |
| **`expire_stale_video_sessions()` and pg_cron scheduling** | Phase 2 audit | `20260404195500_phase2_queue_ttl_ready_gate_sync_daily_gate.sql` defines function + `DO $$ ... cron.schedule` | Not in frozen zip | **VERIFIED IN MIGRATION** | High (cron: **best-effort** per migration `EXCEPTION`) | Cleanup is real; **minute cron is environment-dependent** (notice on failure). |
| **`ready_gate_transition` adds `sync` action for polling fallback** | Phase 2 audit | SQL: `p_action = 'sync'` returns JSON (`20260404195500_*`) | N/A | **VERIFIED IN MIGRATION** | Medium | **`src/hooks/useReadyGate.ts` does not call `sync`** — it polls `video_sessions` via `select` + realtime. **Doc overstates wire-up** vs web implementation. |
| **No active `leave_matching_queue` in `src/` or `apps/mobile/`** | Phase 1 + 3 audits | `grep` across `src/`, `apps/mobile/` — no RPC invocation | Unknown for frozen clients | **VERIFIED IN CODE** (current) | High | Supports “active path retired” for **current** tree. |
| **`join_matching_queue` / `find_video_date_match` are deprecated no-ops** | Phase 3 audit | `20260412143000_phase3_legacy_queue_contract_cleanup.sql` | Legacy RPCs existed in frozen types list | **VERIFIED IN MIGRATION** | High | **Queue story changed mid-2026**; old “join queue” mental model is obsolete in SQL. |
| **Server-atomic forfeit / registration cleanup** | Phase 1 audit | `ready_gate_transition` `forfeit` updates `video_sessions` + `event_registrations` in same function (Phase 2 onward) | Partial in earliest `20260311153000` (forfeit did not clear registrations — evolved in Phase 2) | **VERIFIED IN MIGRATION** (current body) | High | Older narrative “atomic cleanup” must mean **post–Phase 1** SQL, not original Stream 2B file alone. |
| **Queued TTL = 10 minutes (`queued_expires_at`)** | Phase 2 audit | `handle_swipe` sets `now() + interval '10 minutes'` when queued; `expire_stale_*` uses same | Column absent until Phase 2 migration | **VERIFIED IN MIGRATION** | High | Audited **TTL story matches** current SQL. |
| **`drain_match_queue` promotes FIFO by `started_at ASC` with `FOR UPDATE SKIP LOCKED`** | INFERRED from code reading | `20260412143000_*` `drain_match_queue` | Ordering may have differed in older rewrites (manifest warns drift) | **VERIFIED IN MIGRATION** | High | Defines **oldest-first** among eligible queued rows for caller. |
| **Golden snapshot lists complete Edge Function set (28)** | `vibely_golden_snapshot_audited.md` §6 | Current: 43 `supabase/functions/**/index.ts` files | Snapshot claims 28 | **CONTRADICTED** (doc stale vs repo enum) | High | **Baseline contamination:** using §6 as exhaustive EF inventory is unsafe. |
| **Golden snapshot §7 lists all important RPCs** | Same doc §7 | Omits `ready_gate_transition`, `video_date_transition` while listing `handle_swipe`, `drain_match_queue` | Those RPCs added in migrations **after** last frozen file | **DOC INCOMPLETE** | High | Same section mixes **pre/post** repair surfaces without marking boundary. |

---

### Claims from user-requested doc paths **not found** in repo

The following paths from the investigation brief **do not exist** under the workspace (search 2026-04-11):  
`vibely_migration_manifest.md` (root — actual: `_cursor_context/vibely_migration_manifest.md`), `vibely_schema_appendix.md` (root — actual: `_cursor_context/vibely_schema_appendix.md`), `Native_Build_Beggining_Handoff.md` (typo — `Native_Build_Beginning_Handoff.md` exists under `_cursor_context/`), `vibely-source-of-truth-consolidated-2026-03-24.md`, `vibely-master-reference-consolidated.md`, `vibely-project-reference-march2026.md`, `vibely_canonical_project_reference_GPT.md`, `vibely_canonical_project_reference_March23.md`.  

**Implication:** any audit citing those filenames literally cannot be verified without the files; **`docs/vibely-canonical-project-reference.md`** and **`_cursor_context/*`** are the locatable equivalents.
