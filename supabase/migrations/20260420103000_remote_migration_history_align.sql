-- Aligns local migration history with the linked Supabase project (version already applied remotely).
-- No schema change: remote recorded this version before this file existed in the repo.
SELECT 1;
