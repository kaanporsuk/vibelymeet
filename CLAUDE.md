# Claude Operating Note

For any Vibely Video Date, Ready Gate, Daily.co, event-lobby handoff, notification outbox, or post-date survey task, start with:

- `docs/video-date-success-command-center.md`
- `docs/active-doc-map.md`
- `AGENTS.md`

Update `docs/video-date-success-command-center.md` after every material investigation, code change, migration, deployment, manual test, or newly observed failure. Do not treat static tests or partial route entry as proof of recovery; the bar is a fresh end-to-end successful Video Date run from match through post-date survey completion.

Current implementation baseline: functional Video Date code landed in PR #1200 at merge commit `fbca4996a096273914ee650b556ba7994477aa5e`, building on PR #1199 (`ebe4690467b7956511338d94c5847b88889cd1a8`), PR #1196 (`359fa5c42bd5fcdefef9a8a1fca9396d96194f4f`), and PR #1194 (`0a160cd975d87cd756e9c399e748810508f005cb`). PR #1200 adds Supabase migration `20260605115657_video_date_early_confirmed_encounter_promotion.sql` on top of `20260604142017_video_date_active_presence_join_guard.sql`, `20260604170438_video_date_warmup_reconnect_stability.sql`, `20260604193140_video_date_latest_presence_grace_repair.sql`, `20260604205645_video_date_remote_seen_latest_state.sql`, and `20260605085010_video_date_confirmed_encounter_deadline_rescue.sql` for project `schdyxcunwcvddlcshwd`; later terminal-survey lifecycle work adds migrations through `20260605152058_video_date_pending_survey_registration_repair.sql`; latest surface-owner recovery adds `20260605170249_video_date_surface_owner_outer_failsoft.sql` and corrective `20260605174703_video_date_vibe_question_outer_base_name_repair.sql`. Verify Git and cloud state before assuming it. The latest stabilization work adds web/native Daily start ownership, ref-backed same-session Daily lifecycle preservation, lifecycle soft-signal handling, latest-state presence, canonical remote-seen latest-state repair, immediate confirmed-encounter promotion to `date`, reconnect grace clearing/recheck, terminal-survey hard-stop changes, confirmed-encounter deadline fallback rescue, positive launch-evidence extension, surface-claim backoff, active video/survey route ownership, and exposed lifecycle RPC outer fail-soft wrappers. This is not acceptance proof; a fresh manual two-user run is still required.

Do not equate `both_ready`, route entry, Daily room creation, brief warm-up UI, or stale `participant_*_joined_at` with a successful Video Date. Active Daily co-presence requires latest joined presence for both users without a later `participant.left` / `participant_*_away_at`; verify webhook ledger rows, remote media evidence, `date_started_at`, and survey completion. Same-session Daily calls in joining/joined state should be reused or waited on, not rebuilt, and web unmount cleanup must preserve live same-session Daily continuity during route/state churn. Browser `visibilitychange` is soft telemetry during active Daily handoff/warm-up/date. Daily `participant-left` should only reach backend partner-away after local transport grace expires with `p_reason = daily_transport_grace_expired`. If both sides have confirmed remote media, `mark_video_date_remote_seen` or `video_session_handshake_auto_promote_v2` should promote the session to `date` immediately; inspect `finalize_video_date_handshake_deadline` only as fallback rescue with positive launch-evidence extension behavior. Hydrated active `video` sessions, including `queue_status = in_survey`, own `/date/:sessionId` across web/native/mobile; lobby and Ready Gate surfaces must yield instead of preparing Daily or reopening stale Ready Gate, and terminal-survey recovery must force past duplicate-navigation/manual-exit suppression on web and native while preserving same-route no-op protection. Survey-required terminal truth should hard-stop Daily/surface churn and open `PostDateSurvey` on `/date/:sessionId`. Lifecycle RPCs should be outermost fail-soft so stale/duplicate/terminal calls return retryable JSON rather than raw 500s.
