# Claude Operating Note

For any Vibely Video Date, Ready Gate, Daily.co, event-lobby handoff, notification outbox, or post-date survey task, start with:

- `docs/video-date-success-command-center.md`
- `docs/active-doc-map.md`
- `AGENTS.md`
- `CODEX.md`
- `CLAUDE.md`

Update `docs/video-date-success-command-center.md` after every material investigation, code change, migration, deployment, manual test, or newly observed failure. Do not treat static tests or partial route entry as proof of recovery; the bar is a fresh end-to-end successful Video Date run from match through post-date survey completion.

Current implementation baseline: PR #1218 merged at `a7b8cb7dc05a47262a4c7c7dcd31e5972ed4d0c4`, building on PR #1216 (`3ae7f196749f2229d66da6f0ef73ae2f76f30768`) for provider-authoritative presence, PR #1212 (`0a85449a0384f257d314a77c5a7fe455a71e2003`) for shared entry/Daily owner and stable-copresence gating, PR #1213 (`a3c34dd2b2400908c3cf529d8c3146a141b7ebb8`) for rollout documentation, and the earlier PR #1194-#1200 recovery chain, plus the PR #1205-#1216 review-comments follow-up. Supabase project `schdyxcunwcvddlcshwd` is applied/aligned through `20260606224200_video_date_provider_terminal_recovery.sql`, after `20260606180000_video_date_stable_copresence_handshake_guard.sql`, `20260606203000_video_date_provider_authoritative_presence.sql`, `20260606205211_video_date_provider_participant_id_presence_repair.sql`, and `20260606212727_review_comments_1205_1216_followups.sql`. Verify Git and cloud state with `SUPABASE_CLI_TELEMETRY_OPTOUT=1 supabase migration list --linked`, `SUPABASE_CLI_TELEMETRY_OPTOUT=1 supabase db push --linked --dry-run`, and live catalog markers before assuming it. The latest stabilization work adds shared date-entry ownership, route/session Daily ownership, `mark_video_date_daily_alive(...)` owner heartbeats, service-only `video_date_presence_events`, provider-authoritative stable copresence, Daily alive join-stamping only with current provider proof, `provider_participant_id`-first Daily webhook identity extraction, web/native/mobile `owner_state='joined'` only from Daily `joined-meeting` plus local provider session id, no-provider Daily alive calls bounded to throttled telemetry, first provider-backed join evidence preservation, client-side Daily alive RPC skips until local provider proof exists, terminal heartbeat shutdown on server/provider truth, terminal-survey recovery from authenticated `queue_status='in_survey'` registrations when the session-row fetch fails, caller-bound confirmed-encounter promotion, success-aware surface-claim audit derivation, event-wide inactive Ready Gate cleanup before decisive mark-ready delegation, visible-empty-state Mystery Match polling, and native prejoin waiting for surface client identity. This is not acceptance proof; a fresh manual two-user run is still required.

Latest documentation/guidance syncs started with PR #1219 (`849fc3ed5bbec87cf8575fd217a58e8ed3db9834`) and PR #1220 to correct stale handoff guidance after the CTO audit. Docs-only PRs can advance source `main` without changing the PR #1218 implementation baseline; verify exact nested source with `git rev-parse HEAD` and `git ls-remote origin refs/heads/main`. The parent workspace has no remote and tracks `Git/vibelymeet` as a nested gitlink; verify it with `git ls-tree HEAD Git/vibelymeet` instead of hardcoding a local parent pointer hash.

Do not equate `both_ready`, route entry, Daily room creation, brief warm-up UI, visible short media, stale `participant_*_joined_at`, route-owner heartbeats, PR checks, or Supabase alignment with a successful Video Date. Active Daily co-presence requires current provider-backed presence for both users without a later matching `participant.left` / `participant_*_away_at`; verify `video_date_daily_webhook_events.provider_participant_id`, `video_date_presence_events.provider_session_id`, webhook ledger order, remote media evidence, `date_started_at`, and survey completion. Same-session Daily calls in joining/joined state should be reused or waited on, not rebuilt, and web unmount cleanup must preserve live same-session Daily continuity during route/state churn. Browser `visibilitychange` is soft telemetry during active Daily handoff/warm-up/date. Daily `participant-left` should only reach backend partner-away after local transport grace expires with `p_reason = daily_transport_grace_expired`. If both sides have confirmed remote media and current provider proof, `mark_video_date_remote_seen` or `video_session_handshake_auto_promote_v2` should promote the session to `date` immediately; inspect `finalize_video_date_handshake_deadline` only as fallback rescue with positive launch-evidence extension behavior. Hydrated active `video` sessions, including `queue_status = in_survey`, own `/date/:sessionId` across web/native/mobile; lobby and Ready Gate surfaces must yield instead of preparing Daily or reopening stale Ready Gate, and terminal-survey recovery must force past duplicate-navigation/manual-exit suppression on web and native while preserving same-route no-op protection. Survey-required terminal truth should hard-stop Daily/surface churn and open `PostDateSurvey` on `/date/:sessionId`. Lifecycle RPCs should be outermost fail-soft so stale/duplicate/terminal calls return retryable JSON rather than raw 500s. If review feedback arrives after a migration has been pushed to Supabase cloud, add a corrective migration rather than editing applied migration history, then rerun migration list, dry-run, and live catalog marker checks.
